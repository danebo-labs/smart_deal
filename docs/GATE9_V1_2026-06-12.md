# Gate 9R V1 — 2026-06-12

## Verdict

**FAILED — stop after Block B. Do not start Block C.**

The paid cohort completed under budget and passed telemetry, photo, query-profile,
and fallback gates. It failed the manual completeness gate because page 6 returned
`end_turn` with invalid JSON and was degraded by `ChunkMergerService`.

Execution window: `2026-06-12T17:55:33-04:00` to
`2026-06-12T18:06:38-04:00`.

## Cohort

- Manual Batch: 24 pages, SHA-256 `852f508d...c064`.
- Sync PDF: 3 pages, SHA-256 `60a75bf...066`.
- Photos: 8 unique binaries; 7 Sonnet and 1 Opus.
- Queries: 6 logical turns covering top-k `3/5/8/10/15` plus one
  filtered-to-global fallback.
- No S3 chunk writes, Knowledge Base sync, or index mutation occurred. Generated
  chunks were kept in memory.

Photo dimensions:

- Seven JPEG pages: `745x1052`, 55–106 KB, Sonnet.
- Dense schematic PNG: `1414x1996`, 1,579,714 bytes, Opus.

The photo cohort consists of rendered manual pages plus one schematic. It validates
routing and accounting, but it is not representative enough to publish a production
photo p95.

## Cost

| Stage | Cost |
|---|---:|
| Manual 24pp Batch, including page filter | `$0.839733` |
| Sync PDF 3pp, including page filter | `$0.160220` |
| Seven Sonnet photos | `$0.167613` |
| One Opus photo | `$0.188804` |
| Six queries / seven Bedrock generations, CloudWatch tokens | `$0.063250` |
| **Reconciled total** | **`$1.419620`** |
| Authorized cap | `$1.500000` |

The preflight estimate was `$1.36`; actual reconciled cost was 4.38% higher and
left `$0.080380` of budget.

The application ledger reported `$1.400808`. Direct Anthropic and Batch usage was
exact provider usage. Bedrock query input was estimated from observable citations:

| Query metric | Application | CloudWatch |
|---|---:|---:|
| Input tokens | 22,308 | 41,185 |
| Output tokens | 4,426 | 4,413 |
| Query cost | `$0.044438` | `$0.063250` |

The application underestimated query cost by 29.7%. Across the whole V1 cohort,
the ledger underestimated reconciled cost by 1.33%. CloudWatch reported six
`Invocations`, while the application recorded seven generation calls; the app rows
correctly show the filtered attempt and global fallback separately. This count
discrepancy remains unresolved.

## Telemetry

- 45 billable rows.
- Routes: 3 `page_filter`, 24 `batch`, 11 `sync`, 4 `rag_filtered`,
  3 `rag_global`.
- Every row had route, attempt, max tokens, and correlation ID.
- No duplicate invocation signatures.
- Stop reasons: 38 `end_turn`; zero `max_tokens`.
- Batch prompt cache: 4,242 read tokens and 101,864 creation tokens. Only one of
  24 page requests produced a cache read; cache-write churn dominated.
- The six logical queries produced seven rows, with the fallback sharing one
  correlation ID and attempts `1/2`.

## Manual Quality

- 24/24 Batch results succeeded.
- Models: 22 Sonnet pages, 2 Opus pages.
- 62 current chunks versus 72 run4 chunks: 86.1%.
- 211 current field records versus 243 run4 records: 86.8%.
- No invalid or conflicting rendered `FIELD_RECORD` blocks.
- Semantic diagnostic: 41/46 critical records matched at token Jaccard `>=0.5`.
- All six mandatory stop-work records matched, with scores from 0.636 to 0.826.

Exact `RECORD_ID` overlap was only 21/243 because IDs hash generated
action/result/evidence wording. Exact-ID recall is therefore not a valid
cross-inference quality gate. The V1 harness now treats it as diagnostic and uses
semantic critical-evidence matching plus chunk/record retention.

Page 6 remains a real failure. The response ended normally but contained unescaped
quotes inside `chunks[].text`, for example `Consulte la sección "Etiquetas"`.
`JSON.parse` rejected it and the merger emitted a degradation marker. The dormant
manual Batch chain does not retry unparseable results; sync already retries
unparseable output through its 8k/16k/32k ladder.

## Passed Gates

- Reconciled cost below `$1.50`.
- Complete per-invocation telemetry and no duplicate accounting.
- Zero Batch `max_tokens` truncations.
- Photo uniqueness, dimensions, bytes, and 7 Sonnet / 1 Opus routing.
- Query profiles `3/5/8/10/15`.
- Filtered attempt followed by correlated global fallback.
- Sync 3-page PDF: 8 chunks and no degraded pages.

## Required Before Block C

1. Add shared bounded retry for Batch results that are truncated **or invalid
   JSON**, preserving the original Batch row and recording each direct retry once.
2. Re-run the failed manual validation under a new paid authorization. The
   remaining V1 budget is not sufficient for a safe 16k retry worst case.
3. Make commercial query-cost reporting use a CloudWatch-reconciled basis or
   explicitly label per-query input/cost as estimated; citation-only prompt
   reconstruction is not precise enough.
4. Keep V1 red until the manual has zero degraded pages. Do not activate automatic
   long-manual Batch routing yet.

Artifacts retained outside the repository:

- `/private/tmp/gate9_v1_result.json`
- `/private/tmp/gate9_v1_page6_raw.txt`
- `/private/tmp/gate9_v1_evidence_diagnostic.json`
